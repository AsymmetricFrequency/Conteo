{
  "name": "@conteo/workers",
  "version": "0.1.0",
  "private": true,
  "description": "Workers BullMQ: crawler oficial, OCR/visión y disparo de ingesta",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "enqueue:demo": "tsx src/enqueue-demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@conteo/domain": "workspace:*",
    "bullmq": "^5.34.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
