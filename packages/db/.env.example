# Prisma CLI lee DATABASE_URL desde este archivo cuando se corre desde packages/db.
# Debe coincidir con el DATABASE_URL de la raíz (.env).
DATABASE_URL=postgresql://conteo:conteo@localhost:5432/conteo?schema=public
